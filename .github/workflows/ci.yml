- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    pip install pandas pytest torch matplotlib transformers datasets peft the Install dependencies step in .github/workflows/ci.yml as follows:

```yaml
- name: Install dependencies
  run: |
    accelerate
 python -m pip install --upgrade pip
    pip install pandas pytest torch transformers datasets peft accelerate matplotlib
