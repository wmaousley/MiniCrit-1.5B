name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas pytest torch matplotlib transformers datasets peft accelerate
        
    - name: Run tests
      run: |
        pytest tests/ -v
        
    - name: Check data file exists
      run: |
        python -m pytest tests/test_data.py -v
